export default function decorate(block) {
  // eslint-disable-next-line
  const [clinicId] = block.children;
  // eslint-disable-next-line
  const i = 0;
  // eslint-disable-next-line
  const p = i+1;
}
